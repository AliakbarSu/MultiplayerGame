const initialState = {
    exchangeRate: null
}

const RedeemReducer = (state = initialState, action ) => {
    switch(action.type) {
        default: 
            return state;
    }
}

export default RedeemReducer;